<% obj.each do |chef| %>
  <div class="row">
    <div class="col-md-2">
      <section class="chef_info center">
       
      </section>
    </div>
    <div class="col-md-8 well">
      <h4><%= link_to recipe.name, recipe_path(recipe) %></h4>
      <p><%= truncate(recipe.description, length: 150) %></p>
      <p><span class="quiet"><small>Created <%= time_ago_in_words(recipe.created_at) %></small></Span>
      </p>
    </div>
  </div>
<% end %>